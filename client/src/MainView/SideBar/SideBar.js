import React from 'react'
import {css} from '@emotion/core'

const sideBarStyle = css`
  background-color: purple;
  height: 100vh;
`

function SideBar() {
  return <div css={sideBarStyle}>HI</div>
}

export default SideBar
